import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Elite Roofing Pros - Professional Roofing Services in Springfield, IL",
  description: "Elite Roofing Pros provides reliable roofing services in Springfield, IL. Roof replacement, repairs, and gutter installation. Contact us today for quality work and excellent customer service.",
  keywords: ["roofing services", "roof replacement", "roof repair", "Springfield IL", "roofing contractor", "gutter installation"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
